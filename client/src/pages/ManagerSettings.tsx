import { useState } from "react";
import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { AlertTriangle, Building2, Settings2 } from "lucide-react";
import { toast } from "sonner";

export default function ManagerSettings() {
  const { user } = useAuth();
  const settings = trpc.operations.settings.useQuery(undefined, { enabled: Boolean(user && ["super_admin", "hospital_admin"].includes(user.role)) });
  const utils = trpc.useUtils();
  const [department, setDepartment] = useState({ name: "", code: "", description: "" });
  const createDepartment = trpc.operations.departmentCreate.useMutation({ onSuccess: () => { toast.success("Department created."); setDepartment({ name: "", code: "", description: "" }); utils.operations.settings.invalidate(); utils.operations.modules.invalidate(); } });
  const setDepartmentStatus = trpc.operations.departmentSetActive.useMutation({ onSuccess: () => { toast.success("Department status updated."); utils.operations.settings.invalidate(); utils.operations.modules.invalidate(); } });
  const updateRule = trpc.operations.escalationRuleUpdate.useMutation({ onSuccess: () => { toast.success("Escalation timing saved."); utils.operations.settings.invalidate(); } });
  const updateNotification = trpc.operations.notificationRuleUpdate.useMutation({ onSuccess: () => { toast.success("Notification rule saved."); utils.operations.settings.invalidate(); } });

  if (!user || !["super_admin", "hospital_admin"].includes(user.role)) return <Card className="mx-auto max-w-xl border-rose-200"><CardContent className="p-6 text-center"><AlertTriangle className="mx-auto h-8 w-8 text-rose-600" /><h1 className="mt-3 text-xl font-semibold text-slate-900">Manager access required</h1><p className="mt-2 text-sm text-slate-500">Only hospital managers can configure departments, escalation timing, and accountability notices.</p></CardContent></Card>;
  if (settings.isLoading || !settings.data) return <div className="grid gap-4 sm:grid-cols-2">{Array.from({ length: 4 }, (_, index) => <div key={index} className="h-40 animate-pulse rounded-2xl bg-slate-100" />)}</div>;
  const data = settings.data;
  return <><section className="mb-6"><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700">Manager configuration</p><h1 className="mt-1 text-2xl font-semibold tracking-tight text-slate-950 sm:text-3xl">Operations setup</h1><p className="mt-1 max-w-3xl text-sm leading-relaxed text-slate-500">Manage department structure, WhatsApp accountability escalation, and manager notifications. Staff accounts are not used in this operating model.</p></section><Tabs defaultValue="departments"><TabsList><TabsTrigger value="departments">Departments</TabsTrigger><TabsTrigger value="rules">Escalation rules</TabsTrigger><TabsTrigger value="notifications">Notifications</TabsTrigger></TabsList><TabsContent value="departments" className="mt-4"><div className="grid gap-6 xl:grid-cols-[0.9fr_1.3fr]"><Card className="border-slate-200 shadow-sm"><CardHeader><div className="flex items-center gap-2"><Building2 className="h-5 w-5 text-teal-700" /><CardTitle className="text-base">Create department</CardTitle></div><CardDescription>Departments are the destinations for manual WhatsApp task distribution and monthly scores.</CardDescription></CardHeader><CardContent className="space-y-3"><Input value={department.name} onChange={event => setDepartment({ ...department, name: event.target.value })} placeholder="Department name" /><Input value={department.code} onChange={event => setDepartment({ ...department, code: event.target.value })} placeholder="Code, e.g. CSSD" /><Textarea value={department.description} onChange={event => setDepartment({ ...department, description: event.target.value })} placeholder="Operational scope" /><Button className="w-full bg-teal-700 hover:bg-teal-800" disabled={createDepartment.isPending} onClick={() => { if (!department.name || !department.code) return toast.error("Enter a department name and code."); createDepartment.mutate(department); }}>Create department</Button></CardContent></Card><Card className="border-slate-200 shadow-sm"><CardHeader><CardTitle className="text-base">Active departments</CardTitle><CardDescription>Inactive departments are excluded from new WhatsApp task scorecards.</CardDescription></CardHeader><CardContent className="space-y-2">{data.departments.map(item => <div key={item.id} className="flex items-center justify-between gap-3 rounded-lg bg-slate-50 p-3"><div><p className="text-sm font-medium text-slate-800">{item.name}</p><p className="text-xs text-slate-500">{item.code} · {item.description || "No scope note"}</p></div><Button size="sm" variant="outline" disabled={setDepartmentStatus.isPending} onClick={() => setDepartmentStatus.mutate({ departmentId: item.id, active: !item.active })}>{item.active ? "Deactivate" : "Activate"}</Button></div>)}</CardContent></Card></div></TabsContent><TabsContent value="rules" className="mt-4"><div className="grid gap-4 lg:grid-cols-2">{data.rules.map(rule => <Card className="border-slate-200 shadow-sm" key={rule.id}><CardHeader><CardTitle className="text-base">{rule.name}</CardTitle><CardDescription>Manager review timing for pending WhatsApp tasks.</CardDescription></CardHeader><CardContent><div className="grid grid-cols-3 gap-3"><div><Label className="text-xs">First review</Label><Input id={`first-${rule.id}`} className="mt-1" defaultValue={rule.firstReminderMinutes} type="number" /></div><div><Label className="text-xs">Department review</Label><Input id={`head-${rule.id}`} className="mt-1" defaultValue={rule.departmentHeadMinutes} type="number" /></div><div><Label className="text-xs">Admin review</Label><Input id={`admin-${rule.id}`} className="mt-1" defaultValue={rule.adminMinutes} type="number" /></div></div><Button className="mt-4 bg-teal-700 hover:bg-teal-800" disabled={updateRule.isPending} onClick={() => updateRule.mutate({ ruleId: rule.id, firstReminderMinutes: Number((document.getElementById(`first-${rule.id}`) as HTMLInputElement).value), departmentHeadMinutes: Number((document.getElementById(`head-${rule.id}`) as HTMLInputElement).value), adminMinutes: Number((document.getElementById(`admin-${rule.id}`) as HTMLInputElement).value), active: rule.active })}>Save timing</Button></CardContent></Card>)}</div></TabsContent><TabsContent value="notifications" className="mt-4"><Card className="border-slate-200 shadow-sm"><CardHeader><div className="flex items-center gap-2"><Settings2 className="h-5 w-5 text-teal-700" /><CardTitle className="text-base">Manager notifications</CardTitle></div><CardDescription>Configure notices for WhatsApp accountability, outstanding department work, and operational risks.</CardDescription></CardHeader><CardContent className="space-y-3">{data.notificationRules.map(rule => <div className="grid gap-3 rounded-xl border border-slate-200 p-3.5 md:grid-cols-[1.4fr_auto_auto_7rem_auto] md:items-center" key={rule.id}><div><p className="text-sm font-medium text-slate-800">{rule.label}</p><p className="text-xs text-slate-500">{rule.eventType.replaceAll("_", " ")}</p></div><label className="flex items-center gap-2 text-xs text-slate-600"><Checkbox id={`inapp-${rule.id}`} defaultChecked={rule.inAppEnabled} />In-app</label><label className="flex items-center gap-2 text-xs text-slate-600"><Checkbox id={`email-${rule.id}`} defaultChecked={rule.emailEnabled} />Email</label><Input id={`lead-${rule.id}`} className="h-8" type="number" defaultValue={rule.leadMinutes} aria-label="Lead minutes" /><Button size="sm" disabled={updateNotification.isPending} onClick={() => updateNotification.mutate({ ruleId: rule.id, inAppEnabled: (document.getElementById(`inapp-${rule.id}`) as HTMLButtonElement).dataset.state === "checked", emailEnabled: (document.getElementById(`email-${rule.id}`) as HTMLButtonElement).dataset.state === "checked", leadMinutes: Number((document.getElementById(`lead-${rule.id}`) as HTMLInputElement).value), active: rule.active })} className="bg-teal-700 hover:bg-teal-800">Save</Button></div>)}</CardContent></Card></TabsContent></Tabs></>;
}
